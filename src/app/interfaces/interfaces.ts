export interface navbarItem {
    name: string;
    hrefId: string;
}

export interface Experience {
    title: string;
    company: string;
    dates: string;
}

export interface StackLogo {
    src: string;
    alt: string;
    color: string;
}