export interface navbarItem {
    name: string;
    hrefId: string;
}

export interface Experience {
    title: string;
    company: string;
    dates: string;
}

export interface LogoCaroussel {
    src: string;
    alt: string;
}